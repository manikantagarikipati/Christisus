{
  "name": "christisus-web",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "@vitejs/plugin-react": "^4.2.0",
    "typescript": "^5.3.0",
    "vite": "^5.0.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:gh": "tsc && vite build --base /Christisus/",
    "preview": "vite preview",
    "deploy": "npm run build:gh && node -e \"require('fs').copyFileSync('dist/index.html','dist/404.html')\" && npx gh-pages -d dist"
  }
}
